<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scancode">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScanCode">

        <activity
            android:name=".Create.createactivity.QRImageActivity"
            android:exported="false"/>
        <activity
            android:name=".Setting.Main_introduction"
            android:exported="false">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Create.createactivity.EmailActivity"
            android:exported="false"/>

        <activity
            android:name=".Create.createactivity.SMSActivity"
            android:exported="false"/>

        <activity
            android:name=".Create.createactivity.WifiActivity"
            android:exported="false"/>

        <activity
            android:name=".Create.createactivity.LinkActivity"
            android:exported="false"/>

        <activity
            android:name=".Create.createactivity.ContactActivity"
            android:exported="false"/>

        <activity
            android:name=".Create.createactivity.TextActivity"
            android:exported="false"/>

        <activity
            android:name=".Scan.ResultScan"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>